Dockerfile - файл с набором инструкции для создания образа.
Пример:
FROM Ubuntu - определили базовый образ (строго обязательна как первая инструкция)
RUN apt-get update && apt-get install -y cowsay - выполнение команд внутри контейнера (флаг -y нужен, чтобы игнорировать вопросы об объеме файлов)
